# ============================================================================
# OpenCastor Environment Configuration
# ============================================================================
# Copy this file to .env and fill in the values you need.
#   cp .env.example .env
#
# Only uncomment and set the providers/channels you plan to use.
# ============================================================================

# --- AI Providers (The Brain) ------------------------------------------------
# At least ONE provider key is required. Set the key for whichever LLM you
# select in your .rcan.yaml config (agent.provider).

# Google Gemini (Recommended for vision/multimodal)
# GOOGLE_API_KEY=

# OpenAI GPT-4o (Best for instruction following)
# OPENAI_API_KEY=

# Anthropic Claude (Best for complex reasoning/safety)
# ANTHROPIC_API_KEY=

# OpenRouter (Access many models through a single key)
# OPENROUTER_API_KEY=

# Local Ollama (No key needed - just the URL)
# OLLAMA_BASE_URL=http://localhost:11434

# --- Messaging Channels ------------------------------------------------------
# Enable any channels you want the robot to receive commands from.
# Each channel requires its own bot token or credentials.

# WhatsApp (via Twilio)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Telegram Bot
# TELEGRAM_BOT_TOKEN=

# Discord Bot
# DISCORD_BOT_TOKEN=

# Slack Bot
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...
# SLACK_SIGNING_SECRET=

# --- API Gateway --------------------------------------------------------------
# The built-in FastAPI server for remote control and channel webhooks.

# Secret token for authenticating API requests (generate with: openssl rand -hex 32)
# OPENCASTOR_API_TOKEN=

# Gateway bind address (default: 127.0.0.1 for local only, 0.0.0.0 for LAN)
# OPENCASTOR_API_HOST=127.0.0.1

# Gateway port (default: 8000)
# OPENCASTOR_API_PORT=8000

# --- Hardware -----------------------------------------------------------------
# Override default hardware port paths.

# Dynamixel serial port (default: /dev/ttyUSB0)
# DYNAMIXEL_PORT=/dev/ttyUSB0

# Camera device index (default: 0)
# CAMERA_INDEX=0

# --- Runtime ------------------------------------------------------------------
# RCAN schema location (auto-set in Docker)
# RCAN_SPEC_PATH=/usr/local/share/rcan

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
